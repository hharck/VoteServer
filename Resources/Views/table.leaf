<table class="#(tableClass)">
    #if(count(tableHeaders) != 0):
	<tr>
	#for(head in tableHeaders):
		<th>#(head)</th>
	#endfor
	</tr>
	#endif
    
	#for(row in rows):
		<tr class="#(rowClass)">
		#import("tablerow")
		</tr>
	#endfor
</table>
